# 2026-08-12

## OSkhQuant 吸收进 strategy_lab（收尾 + 回归修复）

- 修复 `trading_cost.apply_slippage` 的回归：原实现对 `ratio=0` 仍把成交价 `round(price, price_decimals=2)`，
  导致 10.0909 这类真实收盘价被舍成 10.09，使既有的 `kdj_macd_cross` 等 4 策略 pnl 偏移约 23 元
  （`test_kdj_macd_cross.py::test_t1_and_fee_in_pnl` 失败，差 22.76）。
  修复：滑点实际无调整（ratio=0 / tick_count=0）时直接返回原始价，不做 2 位舍入；仅在有滑点时舍入。
  向后兼容口径不变（旧 `_fee_cost/_fee_proceeds` 直接用原始收盘价计费）。
- 新增回归测试 `test_no_slippage_keeps_raw_price_precision`（tests/test_trading_cost.py）。
- 验证：`test_trading_cost + test_trading_calendar + test_strategies_smoke + test_kdj_macd_cross` 共 28 项全绿；
  `dual_ma`/`rsi` 经 `load_strategy_by_arg` 正常加载并自动注册（注册表 6 个策略）。
- 其余 2 个失败（test_data_autofill_on_backtest）为 `datasource/provider.py` 的 ValueError（unpack）与日期类型比对，
  属既有/环境问题，与本吸收工作无关。
