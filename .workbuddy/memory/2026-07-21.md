# 2026-07-21 工作日志

## 删除 kdj_macd_dual_entry 策略路径B（收尾）
- 延续昨晚任务：用户要求删掉「周线MACD+日线KDJ双入口做T」策略的路径B（左侧抄底分支）。
- 昨晚第一轮工程师（software-engineer）**假完成**：声称完成但磁盘文件未改动（Grep 仍命中 path_b/wk_entry_b 等）。原因疑似 agent 框架瞬态错误。
- 重派第二位工程师（software-engineer-2），带精确 old_string + 强制 Grep/Read 自检要求，真实落盘成功。
- 主理人亲自核验：Grep 全局零命中；py 的 describe/run 中 pb 变量、wk_entry_b 计算、merge 列、空仓段路径B建仓块已删，第251行 equity_curve.append 收尾保留，路径A/做T/清仓完好；toml 的 [params.entry.path_b] 段已删、description 已改写为仅路径A。
- QA（software-qa-engineer）回归：4/4 通过，路由判定 NoOne。新建测试 `tests/test_kdj_macd_dual_entry_pathb_removed.py`（场景A验证路径A建仓正常；场景B验证"仅旧路径B特征+日线J≥50"时不建仓=0笔，证路径B确已移除）。
- 顺手修正模块 docstring 第5行"两条平行入口"→"单入口路径A"，消除误导性措辞。
- 涉及文件：`src/strategylab/engine/strategies/kdj_macd_dual_entry.py`、`src/strategylab/resources/strategies/kdj_macd_dual_entry.toml`、`tests/test_kdj_macd_dual_entry_pathb_removed.py`。
- 未 git 提交、未重启服务。

## 修复回测"行情缺失/不足"死循环（自动补足）
- 用户报：不管选什么日期回测都显示"行情缺失/不足，请点更新数据源"，但昨天已全市场更新过。
- 根因：① 更新数据源 genesis 写死（日线20220706/周线20211210，batch_runner.py:176-177 + provider.py 默认）；② 增量窗口 `_incremental_window`(cache.py:140) 只看 meta.last，已更新股票 last>=today 就跳过取数，**永不往前补**；③ 回测 required_beg=start-1年(backtest.py:45-46)，早于2022-07即不足。→ 更新只从2022-07往后补，永远补不到 warmup 历史，形成死循环。
- 实证：600839.SH/600000.SH 的 eastmoney 日线 meta first=20220706,last=20260720（更新确实成功，只是起点晚）。
- 用户决策：回测行情不足时**自动补足再回测**（不要只提示手动更新）。
- 修复（software-engineer，IS_PASS YES，主理人核验落盘）：
  - cache.py `_incremental_window` 改为双向补（need_front=first>default_beg→返回default_beg；need_back=last<today→last+1；都不缺→None）。
  - provider.py 新增 `_cache_needs_front` helper，update 分支两处旧格式"已最新跳过"判断加 `and not daily_need_front` 守卫（防往前补被误跳过）。
  - backtest.py import DataMissingError，verify 失败自动 except 内 `ensure_data(mode="update")` 补足后继续；update 仍失败则保持上浮。
- QA（software-qa-engineer）回归：6/6 通过，路由 NoOne。新建 `tests/test_data_autofill_on_backtest.py`（用例A双向补4断言、用例B update 往前补端到端 mock、用例C run_symbol verify→update 契约）。backtest.py 覆盖率98%。
- 注意：TeamCreate 工具本次会话不可用，改用 Agent 直接派工（software-engineer / software-qa-engineer），实质协作不变。
- 未 git 提交、未重启服务。

## 修复 eastmoney 源空响应崩溃 (AllSourcesFailedError → AttributeError None.get)
- 用户报：回测自动补足 600839.SH 时报 `AllSourcesFailedError: [ALL] 所有数据源均失败 symbol=600839.SH: eastmoney: AttributeError: 'NoneType' object has no attribute 'get'`。
- 根因：`eastmoney.py:77` 原 `data.get("data", {}).get("klines", [])` —— 东财接口在限流/区间无效/偶发空响应时返回 `{"data": null}`，`data.get("data", {})` 拿到 None（键存在值为null不触发默认值），再 `None.get("klines")` 崩。且接口返回 null 时会静默返回 `[]` → 下游 merge 写入空数据 → 回测仍"行情不足"且不报错（静默失败）。
- 修复（software-engineer，IS_PASS YES，主理人核验落盘）：eastmoney.py `_raw_fetch_kline` 健壮化——网络/HTTP错误、响应非JSON、响应非dict、data.get("data")非dict 均明确抛 `DataSourceError`（带 rc/rt/raw 摘要）；正常时 `payload.get("klines") or []`。顺带 urlopen timeout 接入 `getattr(self.config,"network_timeout",30)`（config 默认15.0，原硬编码30）。未动 base/provider/cache/backtest。
- QA（software-qa-engineer）回归：8/8 通过（用例A/B/C/D/E/F），eastmoney.py 覆盖率100%，路由 NoOne。新建 `tests/test_eastmoney_null_response.py`（纯 mock urlopen，覆盖 null/正常/非JSON/非dict/网络错误/配置接入）。
- 回归发现陈旧测试：`tests/test_datasource.py::test_fetch_kline_no_data_field` 原断言 `{"code":0}`（无data字段）返回空DataFrame，现应抛 DataSourceError。派第二个 QA（software-qa-engineer-2）更新该用例为 `pytest.raises(DataSourceError)` 并跑全量（11min）报告绿。主理人亲自核验：该用例已改（277-278行）；单跑验证 PASS（抛 DataSourceError）。另注意 `test_datasource.py` 整文件 pytest 极慢（疑似含网络/DB fixture），亲自核验时应只跑纯 mock 文件或单跑用例逻辑，避免整文件卡住。
- 未 git 提交、未重启服务。

## 修复 eastmoney 回测取数 rc=100 永久失败（http→https + 可重试 + 分页）
- 用户报：删路径B/自动补足/空响应三轮修复后，回测 600839.SH 仍报 `AllSourcesFailedError: eastmoney 返回空数据 payload=None; rc=100 rt=1`（上一轮空响应修复让它从 AttributeError 变成清晰 DataSourceError，但东财真的拒绝了请求）。
- 根因定位（主理人亲自实测复现）：
  1. **确定性元凶**：`eastmoney.py` 的 `API_URL` 是明文 `http://push2his.eastmoney.com/...`，东财现已主推 https，明文 http 被拒/重定向，稳定返回 `rc=100`。主理人用受管 Python 实测：`https://` + 同款参数 → `rc=0` 正常返回 2019 至今数据；`http://`/连发 → `RemoteDisconnected`/被掐。
  2. **重试缺失（放大）**：上一轮把"接口返回 null/rc≠0"抛成 `DataSourceError`，而 `base.py` 的 `_is_retryable_network_error` 只把网络层错误判可重试，`DataSourceError` 不在其列 → `fetch_kline` 重试循环 `retryable=False` → 立刻致命抛出、零重试零退避。`rc=100` 本是限流/瞬时拒绝，应可重试。
- 修复（software-engineer，主理人亲自 Read 核验落盘）：
  - exceptions.py 新增 `RetryableDataSourceError(DataSourceError)` 子类。
  - base.py `_is_retryable_network_error` 开头加 `if isinstance(exc, RetryableDataSourceError): return True`。
  - eastmoney.py：`API_URL`→`https://`；import 加 `RetryableDataSourceError`+`datetime/timedelta`；`_raw_fetch_kline` 重写为分页取数（按 `_CHUNK_DAYS=1460` 切4年窗，逐段取数按行首日期去重合并、升序返回），新增 `_fetch_chunk`（网络错误/非JSON/非dict/rc≠0/payload非dict 一律抛 `RetryableDataSourceError`；URL 加 `ut=fa5fd1943c7b386f172d6893dbfba10b`+`Host: push2his.eastmoney.com` 头；保留 fqt=1）。未动 base/provider/cache/backtest 取数补数逻辑。
- QA：`software-qa-engineer` 首次跑回归时遇**平台 500 基础设施故障**（非测试失败、非代码问题）→ 重派 `software-qa-engineer-2` 接手。最终：新建 `tests/test_eastmoney_rc100_retry.py`（5用例：A可重试识别/B分页去重升序/C rc=100重试后成功/C2重试耗尽边界/D https URL+ut+Host），与既有 `test_eastmoney_null_response.py`(8) 一并运行 **13 passed**，路由 NoOne。主理人亲自 pytest 复跑 13 passed 确认落盘。
- 未 git 提交、未重启服务。
- **累计未重启提示**：本会话 4 轮修复（删路径B、回测自动补足、eastmoney空响应、eastmoney rc=100）均改代码但未重启服务，需用户跑 `restart.bat` 生效。
