# 2026-08-17 工作日志

## RSI 超买超卖策略增强
- RSI 判断周期由 14 改为 12（`rsi.py` describe/run 默认值 + `rsi.toml` `rsi_period=12` 早已一致）。
- 新增成本价回撤 ≥5% 硬止损：新增 `stop_loss_pct`(默认0.05) 参数；持仓期 `low <= entry*(1-5%)` 触发，**优先于**超买信号清仓。
- 用户后续要求止损改为「盘中最低价即时成交」：止损分支成交价 `close` → `low`（当日最低价）；`describe` 同步披露。买入/超买清仓仍用当日收盘（含 look-ahead 偏差），止损分支消除 look-ahead。极端暴跌时实际亏损可能略超阈值（取决于当日振幅），已在 describe 声明。
- 验证：冒烟测试 3 passed；构造数据（阴跌30天→强反弹买入95→次日 low=52≤90.25）确认止损以当日最低价 52 成交，非收盘 60。

## 环境踩坑（临时验证脚本路径）
- win32 + Git Bash 下：`/tmp` 被解析为 `E:\tmp`；Write 工具写 `C:\tmp` 绝对路径后 Python 报找不到；safe-delete 不支持带盘符绝对路径（`rm` 会被拼到 cwd 下）。
- 对策：临时验证脚本落地用 `$USERPROFILE/AppData/Local/Temp/v.py`，删除用 `/c/Users/Administrator/AppData/Local/Temp/v.py`（Git Bash 原生绝对路径）形式。
