# 2026-07-18

## Strategy Lab 回测结果存储改造（文件 → 数据库）
- 用户诉求：策略可维护（已满足：toml+注册表）、按标的时间策略回测、结果可比对、把"跑的数据"从文件(三件套 CSV/JSON)改为数据库存储（愿提供连接信息）。
- 现状缺陷：三件套文件名只按标的命名、不含策略/区间 → 同标的换策略/区间重跑会覆盖；web 每次覆盖 index.html，仅同批次内比对，无法跨历史回测比对。
- 架构师(高见远)已产出设计：docs/system_design.md / docs/sequence-diagram.mermaid / docs/class-diagram.mermaid。方案=SQLAlchemy 2.0 DB无关，run_id=UUID，4表(backtest_runs/equity_points/trades/summary)，新增 engine/storage/ 包，web 增 /history+/api/runs+/compare，任务 T01~T05。
- 用户决策：① MySQL；② 只存DB、不双写文件；③ 网页历史页多选对比；④ 权益曲线起点 rebased 到100。
- 进行中：工程师(寇豆码)实现 T01~T05，本地用 SQLite 验证 DB 无关代码；待用户提供 MySQL 连接串后切到 MySQL 验证。

## 验证与清理完成（当日后续）
- MySQL 复验全绿：连接串 `mysql+pymysql://root:root@localhost:3306/strategylab`（库 `strategylab` 已 CREATE DATABASE；MySQL 5.7.43）。两次回测落库（run A `799bc214…`、run B `3a1054ac…`）；四表+version 行数 runs=6/equity=2172/trades=76/summary=6/schema_version=[(1,1)]；跨回测对比 HTML 含「跨回测对比」+rebased=100；Web `/api/runs` `/history` `/compare` 均 200。SQLite 单测 5/5 早前已过。
- 关于"要不要手动建表"：不需要。SQLAlchemy `create_all`(init_db) 首次运行自动建 5 张表；用户只需手动 CREATE DATABASE（SQLAlchemy 不能建库本身）。
- 代码清理（用户要求"来落库的落库"+专业 SWE 规范）：
  - 删除 12 个历史输出三件套（*_equity.csv/_trades.csv/_summary.json，4 标的各一套）——已落库，纯残留。
  - `export_results` 默认 `write_files=True` → 改为 `False`，锁死"只落库不双写"。
  - `backtest.py` 删除死代码 `_patch_trades_csv`（无调用方、引用 trades.csv）及无用 `import csv`，更新模块文档字符串。
  - 新建 `.gitignore`：排除三件套/index.html/compare html/*.db/行情缓存/_daily|_weekly.csv/__pycache__/.env。
  - 保留 10 个 `*_daily.csv`/`*_weekly.csv`（行情输入缓存，data_feed 读取、可重取，已 gitignore，非结果不删）。
- 交付报告落盘：`docs/数据库存储改造-交付报告-2026-07-18.md`（含完整使用方法）。
- 环境注记：子代理(SendMessage/Agent)在本机会话中多次因 `copilot.tencent.com` 502 失败；MySQL 复验与清理由主理人直接用本地 Bash+托管 Python 3.13.12 venv 完成。

## src 布局重构（专业 Python 包化）
- 用户要求"按专业 Python 项目架构规范"重构。架构师(高见远)产出 docs/src-layout-refactor-design.md + 2 张 mermaid；决策：src/strategylab/ 布局 + pyproject.toml(PEP621, setuptools, console_scripts: strategylab/strategylab-web) + 删 run.py 的 sys.path hack + python -m strategylab 可用 + 清理垃圾 + MIT LICENSE。
- 子代理 software-engineer-2 在网络掉线前已完成 T01~T05（src/ 全量迁移、cli.py/web.py/settings.py/logging_config.py、resources 内置 toml、tests/conftest.py、pyproject、LICENSE、.gitignore、README 更新）；剩余 T06 清理 + T07 验证由主理人直接本地完成。
- 收尾动作：10 个行情 CSV 从根目录搬进 data/（新缓存基目录 get_data_dir() 默认 ./data，复用成功）；删除根死目录 engine/、strategies/ 及残留 nul_del_tmp、index.html。
- 验证全绿：pytest 5/5；`python -m strategylab --help` 正常；`import strategylab.web` OK；CLI 端到端冒烟 600216.sh 2023-07-18~2024-07-18 落默认 sqlite（runs=1/equity=244/trades=9/summary=1），复用 data/ 缓存跳过取数，index.html 生成于 data/。
- 注：venv 缺 python-dotenv/pytest/pytest-cov，已 `pip install` 补上（网络恢复时）。pyproject addopts 含 --cov（需 pytest-cov）。
- CSV 结论：项目内 CSV 仅剩行情输入缓存（*_daily.csv 日线、*_weekly.csv 周线，data_feed.ensure_data 生成复用），属输入非结果；回测结果已落 DB，不再有 *_equity/_trades/_summary 输出 CSV。

## 文档更新与使用指南（当日后续）
- 用户要求"按照现在项目更新文档 + 添加详细使用指南"（打算分享项目给不懂编程的人）。
- 新建 `docs/使用指南.md`：超详细手把手中文指南，覆盖安装 Python → pip install -e . → 配置 DB → 命令行/网页两种用法 → 历史回测对比 → 改策略参数 → 常见问题 FAQ → 目录速览 → 免责声明；所有命令可直接复制粘贴。
- README.md：新增 §0 "快速上手（30 秒）" + 指向使用指南的链接。
- docs/需求文档.md：更新 FR-01（run.py→strategylab、三件套→落库）、FR-04（strategies/→resources）、FR-07（标准三件套→结果落库 DB）、验收标准、术语表等关键过时引用。
- docs/技术文档.md：更新架构概述（run.py/web_app.py→cli.py/web.py src/布局）、数据流、CLI/Web 流程、API 端点表（新增 /history/api/runs/compare）、部署命令、策略路径、三件套 Schema 标记为已弃用→落库 Schema。
- docs/system_design.md：新增 §0 "实际落地与本文差异（v2 更新）"，注明关键决策偏差（仅落库不双写、src 布局迁移、新增端点）。
