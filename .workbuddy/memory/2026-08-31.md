## 2026-08-31

- 修复 RSI 策略回测提交时的 MySQL 1205（Lock wait timeout exceeded）。
  - 根因：`submit_batch` 在启动新批次前调用 `repository.clear_strategy_runs`，对 RSI 维度一次性 DELETE 729 个 run 的 `equity_points/price_points/trades/summary/batch_items/backtest_runs`；此时并发存在一个正在写入的「指标共振」批次，大事务删除与并发插入在 `equity_points` 索引上发生锁等待。
  - 改动：`src/strategylab/engine/storage/repository.py` 的 `clear_strategy_runs` 改为按 `CLEAR_RUN_CHUNK_SIZE`（默认 500，环境变量 `STRATEGALAB_CLEAR_RUN_CHUNK_SIZE` 可调）分批删除并逐批提交，缩小单事务锁范围。
  - 验证：`test_v3_features.py::test_clear_strategy_runs_keeps_data_batch`、`test_batch_runner.py::test_submit_batch_hit_and_pending` 通过；在真实 MySQL 上清除了 729 条 RSI 历史 run，未再触发 1205。
