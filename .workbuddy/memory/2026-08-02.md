# 2026-08-02 工作日志

## 1. 分析查询页新增"策略统计分析"卡片（快速模式：工程师实现 + QA 验证）
- **需求**：`/analysis` 页加统计框，展示当前查询条件下已跑股票分布：胜率>50% 占比、|最大回撤|≤10%/15%/20% 占比、夏普>1 占比。
- **实现（web.py 单文件）**：
  - HTML 卡片 944-948 行（排名卡上方，样式同现有 card）
  - `computeStrategyStats(items)` 纯计算函数 1109-1127 行：胜率 `win_rate_pct > 50`、回撤 `Math.abs(max_drawdown_pct) <= 10/15/20`（abs 兼容正负符号）、夏普 `sharpe > 1`；分母=指标有效（非 null/NaN）的股票数
  - `renderStrategyStats` 1130-1156 行：百分比(1位小数)+分子/分母+进度条+有效数据数标注；分母 0 显示 "-"
  - `loadStats` 1159-1191 行：**全量翻页拉取** `/api/rank?size=500` 直到 total（最多 200 页），`statsSeq` 丢弃过期请求；`loadStats()` 在 loadRank 成功回调 1063 行触发
  - CSS 899-908 行（stat-block/stat-item/bar/fill/hint/frac）
- **QA 关键核实（重要经验）**：
  - `/api/rank` items 的指标字段在**顶层**（非 summary 嵌套）：keys=[end, last_buy_date, max_drawdown_pct, run_id, sharpe, stale, symbol, symbol_name, total_return_pct, win_rate_pct]
  - `win_rate_pct` 是 **0-100 量纲**（不是 0-1），`>50` 判断正确
  - `max_drawdown_pct` DB 中**全为正数**（引擎存 abs 值），Math.abs 属防御性写法
  - 真实 DB 5213 条 run；turtle 策略 1301 只全量翻页 3 页收集完整无重复
  - 统计验证：>50胜率 42 只、|dd|≤10 664 / ≤15 1124 / ≤20 1272、sharpe>1 26/1295
- **已修 typo**：web.py:1098 "基于 N 只有效数据" → "基于 N 个有效数据"
- **遗留提示**：`isNumVal` 对 `Number('')===0`、`Number(true)===1` 判有效（当前 API 只返 float|None，不触发；未来字段变字符串需注意）；loadStats 每次查询/翻页/排序都会额外全量翻页（性能可接受，无后端聚合 API）
- **⚠️ 需重启服务生效**：QA 实测线上 8000 端口进程启动早于改动，页面 6 项检查全 MISSING；重启（restart.bat）后访问 /analysis 点"查询排名"即可看到统计卡
- QA 脚本（非源码）：`qa_verify_stats.js`（node 纯逻辑验证）
