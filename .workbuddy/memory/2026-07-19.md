# 2026-07-19 工作日志

## Strategy Lab 多数据源抽象层（团队 software-multidatasource）
- 用户诉求：给平台加多数据源避开东财 IP 限流；优先 akshare+tushare，同花顺/券商 P2 预留，本地通达信因本机未装排除。
- PRD（许清楚）：docs/multidatasource-prd.md，含 P0/P1/P2 需求池、.env+CLI 方案、待确认问题。
- 架构设计（高见远）：docs/multidatasource-arch.md + class/sequence mermaid。模式=抽象基类(模板方法)+适配器+工厂+容灾策略；新建 engine/datasource/ 子包(~13文件)，data_feed.py 退化为兼容层；每源独立限流熔断；缓存文件名带 source；配置 STRATEGALAB_DATA_SOURCE/SYMBOL_SOURCE/FALLBACK_SOURCES/TUSHARE_TOKEN/FAILOVER/CB；CLI datasource status；任务 T1-T10。
- 用户拍板：①运行复用键扩为含 source + 历史回填迁移（backtest_runs 加 data_source 列回填 eastmoney）；②进入实现阶段（走标准 SOP）。
- 工程师实现（寇豆码）：13 个新文件 + 10 个修改文件，IS_PASS: YES，零语法错误。
- QA 回归（严过关）：74/74 全部通过，智能路由 NoOne，零 Bug。
- 交付：服务已启动（http://localhost:8000），使用者可通过 .env 切数据源。
- 后续小改进：收益率颜色统一（正红负绿，web.py CSS）；创建 __main__.py 支持 `python -m strategylab` 启动；创建 restart.bat 一键重启脚本。
- 最终文档同步：CHANGELOG、需求文档、技术文档、使用指南 全部更新，涵盖多数据源、风控、策略变更、板块清理等所有改动。

## 预热取消按钮 + 市值收窄（团队 software-preheat-improve）
- 用户两诉求：①全市场预热时取消按钮失效；②把主板+创业板标的按市值100亿~1000亿收窄。
- 取消按钮根因已定位：web.py:631 取消按钮缺 `id="cancel-btn"` → cancelBatch() 操作 null 抛错 → 取消请求发不出。但经核实该 id 在当前代码中**已存在**（此前已修复），cancelBatch 已有空值保护、beginBatch 已设 curBatchId、后端取消机制(threading.Event)完好。
- QA 严过关独立回归：静态核对 web.py + 起服务实测提交批次后取消→状态 cancelled、轮询停止；IS_PASS YES / 路由 NoOne。故「取消不了」在当前代码已不存在，未改 web.py。
- 市值收窄重大发现：data/sector_stocks.json 当前仅 **1301 只**（非历史所述的 4590），且经百度源(ak.stock_zh_valuation_baidu, 单位亿)逐只核实，这 1301 只市值 min=100.00亿/max=993.01亿/中位数≈193亿，**全部已在100~1000亿区间**→过滤零剔除。用户确认「保持 1301 只」。
- 数据源偏差：akshare 1.18.64 的 stock_zh_a_spot() 无「总市值」列，已改用百度源绕开被封的东财。新建可复现脚本 scripts/filter_by_market_cap.py（自适应数据源+并发+续传缓存+防空写），供市值漂移时重跑。
- 预热宇宙读取链路：web.py:_expand_all_market() → repository.get_sector_stocks() → data/sector_stocks.json（唯一来源，无隐形源）。
- 结论：两项均验证通过，无代码改动需求； universe 已天然合规。

## 分析查询页：加胜率列 + 列头可排序 + 修分页（团队 software-analysis-rank-fix）
- 用户三诉求：①列表加「胜率」列；②列头可点击排序；③修「第1页切第2页无数据」的 bug。约束：用户正在跑数据，**禁止重启/起服务**，只改代码离线验证。
- 根因排查：主理人实测运行中服务 `GET /api/rank?page=2` 返回 50 条（total≈1358~1486），后端 `rank_runs` 分页(offline + live)均正常 → **「第2页空」为纯前端 bug**；同时发现 `rank_runs` 返回 items **本无 `win_rate_pct` 字段**（Summary 表有，但未注入），加列必须前后端同改。
- 工程师改动（寇豆码，IS_PASS YES，未重启）：
  - `engine/storage/repository.py` `rank_runs()`：items 新增 `win_rate_pct`；新增 `sort_by`/`order` 参数，白名单{symbol_name,total_return_pct,max_drawdown_pct,sharpe,win_rate_pct}，None 排末尾，非法/None 回退默认(total_return_pct 降序，向后兼容)。
  - `web.py` `_api_rank()`：解析并透传 sort_by/order 给 rank_runs（csv 与正常分支均透传）。
  - `web.py` `build_analysis_html()`：新增「胜率」列(夏普后、数据时效前)；五列头可点击排序(带▲/▼指示、金色高亮)；`loadRank(page)` 页码守卫 + fetch 始终带 sort 参数 + 空数据守卫 + 翻页器必渲染；`sortBy(col)` 同列切向/异列新列(名称默认asc)；`openRun` 改用 data-rid 规避引号转义。
- QA 严过关独立回归：离线 rank_runs 验证(胜率字段出现/5字段方向/None末尾/非法回退/分页切片) + 前端静态逐行审查 + py_compile 通过；**IS_PASS YES / 路由 NoOne**，三项全过。
- 遗留建议(非阻塞)：默认排序分支用 `x["total_return_pct"] or 0`，NULL 会按0而非排末尾(与显式sort_by不一致)，当前数据无NULL无影响；建议补 win_rate_pct/sort_by 单测。
- 关键约束记录：改动仅在代码层，**下次重启服务才生效**；当前运行实例不受影响，用户跑数据期间无需操作。

## 详情页明细表 sticky 表头（团队 software-sticky-header）
- 用户诉求：详情页「持仓/交易明细」表格向下滚动时，表头固定显示在上方。
- 根因（主理人定位）：`dashboard_template.html` 中 `.trades-table thead th`/`.position-table thead th` **本已有** `position: sticky; top: 0`，但①两个包裹层 `.trades-table-wrap`(原仅 overflow-x:auto)、`.position-table-wrap`(原仅 overflow-x:hidden) **无 max-height 也无 overflow-y** → sticky 无吸附容器，等于不吸；②表头 `background: transparent` → 吸附后透穿下方行。
- 工程师改动（寇豆码，IS_PASS YES，未重启/不起服务）：仅改 `src/strategylab/engine/vendor/dashboard_template.html` 3 条 CSS 规则：
  - `.trades-table-wrap { max-height:62vh; overflow-x:auto; overflow-y:auto; }`
  - `.position-table-wrap { max-height:62vh; overflow-x:hidden; overflow-y:auto; }`
  - `.trades-table thead th,.position-table thead th { background: var(--surface,#161b22); position:sticky; top:0; z-index:2; box-shadow:0 1px 0 var(--border); }`（background 透明→不透明，保留 sticky）
- QA 严过关独立静态核验：核对 3 处改动落点、CSS 语法括号配平、HTML 结构完整、`--surface`(#161b22 dark / #ffffff light)与页面协调无突兀色差；**IS_PASS YES / 路由 NoOne**。
- 两点须知(非缺陷)：①工作区有与本次无关的在途改动(web.py/repository.py/kdj_macd_dual_entry.py/docs 等，为前序任务遗留)；②`data/index.html` 是旧生成产物，sticky 需**重启/重渲仪表盘后才生效**。
- 约束延续：本次未重启，**改动下次重启生效**，运行实例不受影响。

## 文档同步：今日三项改动入档（团队 software-doc-sync）
- 用户诉求：把今日（下午，文档 11:40 后）的三项代码改动同步进项目文档，并校正过时标的数量。
- 产品经理许清楚执笔（纯文档，未动代码、未重启），同步 4 份文档：
  - `docs/CHANGELOG.md`：新增「八、2026-07-19 网页交互与全市场预热增强」章节，**§29**（分析页胜率列+排序+分页修复）、**§30**（详情页 sticky header 根因与修复）、**§31**（市值过滤脚本+预热宇宙1301只口径）。
  - `docs/技术文档.md`：§15.3 `/api/rank` 行补 `sort_by`/`order` 白名单 + 返回 `win_rate_pct`；§8 补详情页表头 sticky 说明；§15.7 分析页补胜率列+列头排序。
  - `docs/需求文档.md`：FR-20 补 `sort_by`/`order` 与 `win_rate_pct`；FR-21 补胜率列/可排序/翻页修复；FR-08 附近补详情页表头 sticky；FR-15 的「5500只」→「当前预热宇宙 1301 只」。
  - `docs/使用指南.md`：§10.5.2 全市场预热「约5500只」→「31板块约1301只中盘股(已按100~1000亿收窄)」+ 补 filter_by_market_cap.py 用法；§10.5.3 补胜率列/列头排序/翻页修复；§10.1 补表头固定(sticky)提示。
- 数字校正：4 份目标文档内「5500」已无残留（使用指南2处+需求文档FR-15 1处）。
- 范围外遗留（许清楚标注，本次未改）：① `docs/v2.0-design.md:364` 仍有「排名查询命中5500行」；② `sector_stocks.json` 的 5527/4590 表述散见技术文档§3、需求文档FR-09、CHANGELOG §4/§27（属「板块选股候选池」历史口径，与「预热宇宙1301只」非同一语境）。如需全仓统一口径建议另立项。
- 主理人抽查：4 份文档关键落点(grep 命中 CHANGELOG §29/30/31、技术文档 §15.3 L369/§8 L167/§15.7 L404、需求文档 FR-20/21/08、使用指南 §10.5.2 L312/§10.5.3 L329-332/§10.1 L285)全部落地，文档同步完成。

## 新增策略：海龟交易法则（团队 software-turtle-strategy，快速模式）
- 用户诉求：新增「海龟交易法则」策略——日线唐奇安通道突破(收盘价突破20日最高买入)、持仓后跌破10日最低或回撤超2倍ATR止损离场、海龟单位仓位管理。
- 实现（工程师寇豆码，未重启/未起服务）：
  - 新增 `src/strategylab/engine/strategies/turtle.py`：`TurtleStrategy(type="turtle")`，唐奇安通道 `close.rolling(N).max/min().shift(1)`（无未来函数）、ATR `max(h-l,|h-pc|,|l-pc|).rolling(14).mean().shift(1)`、离场优先级先ATR止损后通道离场、输出结构(equity_curve/trade_history/positions)100%对齐 kdj 策略前端渲染。
  - 注册 `src/strategylab/engine/strategies/__init__.py` 的 STRATEGY_REGISTRY；新增 `src/strategylab/resources/strategies/turtle.toml`（name/type/market/description + 全套 params，含 [params.turtle]：entry_lookback=20/exit_lookback=10/atr_period=14/atr_stop_multiple=2.0/use_atr_stop=true/risk_per_trade=0.01/trailing_stop=false）。前端下拉由 list_available_strategies() 扫描此目录自动出现，web.py 无需改。
- QA 严过关 Round1 发现源码 bug：仓位公式 `shares=floor(unit_risk/(atr_i*close_i))` 多除 close_i（量纲错，高价股归零零成交）→ 工程师修正为 `shares=floor(unit_risk/(atr_stop_multiple*atr_i))`（一次2倍ATR止损恰亏1%权益，小ATR由 base_buy 上限兜底）。
- QA Round2 回归 26/26 全绿：IS_PASS YES / 路由 NoOne；高价股场景「0成交→1笔」缺陷消除；无未来函数、字段对齐、离线跑通、边界均正确；下次重启后出现在策略下拉。
- 约束：全程未重启、未起服务、未动运行中进程；新策略仅对下次重启生效。

## 分析查询页：海龟预热后「没有跑过数据」误报（团队 software-bugfix-turtle-prewarm-query，BugFix）
- 用户诉求：选海龟 → 全市场预热 → 进分析查询页选海龟 → 显示「没有跑过数据」，认为有 Bug。约束：用户正在跑数据，**禁止重启/起服务**，只改代码离线验证。
- 根因（主理人实测运行中服务 curl）：后端海龟链路完全正常。POST 海龟预热→建批次 00ab99b3→done=1301/failed=0→`/api/rank?strategy=turtle` 返回 total=1301 真实数据（大金重工+144%、九安医疗+112%等）。用户那次「没数据」是因为**那次海龟预热根本没在系统留任何海龟批次**（此前 `/api/batch` 列表无海龟批次），即预热未真正落库或用户在异步后台跑完前就去查询；而分析页空结果无「预热进行中」引导，造成「出 Bug」误解。
- 修复方向：分析查询页空结果引导 hardening（非策略逻辑修复）。主理人已为用户重跑一次正确海龟预热（批次 00ab99b3-d06a-4357-b5e8-a86efd1ba3e7），实测 done=1301/failed=0，用户刷新即见数据。
- 工程师改动（寇豆码，IS_PASS YES，未重启/不起服务）：
  - `engine/storage/repository.py`：新增 `latest_batch_for_strategy(strategy_name, params_hash=None) -> dict|None`（try/except 兜底，返回 `{status,done_count,total_count,created_at}` 或 None）。
  - `web.py` `_api_rank()`：仅当 `data["total"]==0` 时附加 `warmup`（调 latest_batch_for_strategy）；CSV 分支提前 return 不受影响。
  - `web.py` `loadRank`：空结果分支按 `warmup.in_progress` 显示「预热中(已完成 X/Y)」引导或原「暂无已跑过的回测」文案。
- QA 严过关独立回归：**路由 NoOne（源码无 Bug，全过）**。真实库：海龟批次 status=done/done=1301/total=1301→in_progress=False；不存在策略名→None；params_hash=None 仍查得到；隔离场景+try/except 兜底全 PASS；CSV 提前 return、前端分支推演一致；py_compile 两文件通过。落地可复现脚本 `qa_verify_warmup_realdb.py` + `qa_verify_warmup_mock.py`。
- 约束延续：本次未重启，**前端引导改动下次重启生效**；当前运行实例不受影响，但海龟真实数据主理人已重跑，刷新即见。

## 分析查询页：海龟「查看详情」KeyError: 'entry'（团队 software-bugfix-turtle-detail-entry，BugFix）
- 用户诉求：分析查询选海龟 → 点股票「查看详情」报错 `KeyError: 'entry'`。约束：不重启服务、不动 PID 41088。
- 根因（主理人定位）：`engine/dashboard.py` `_note_texts(cfg)` 写死读 `cfg["params"]["entry"]["path_a"]`（KDJ 双入口专用键）；调用链 分析页详情→`/history?run_id=`→`web._render_single_run`(web.py:1232)→`build_compare_from_runs([run_id])`→`_note_texts`。海龟 toml `type="turtle"`、params 仅 `turtle.*` 子键，无 `entry` → 抛错被 except 捕获返回 HTTP 500 错误页。
- 工程师改动（寇豆码，IS_PASS YES，未重启）：重构 `_note_texts` 按 `cfg.get("type")`(name 兜底) 分发 → `_note_texts_kdj`(原样逐字保留、键改 .get) / `_note_texts_turtle`(唐奇安 shift(1) 无未来函数、先 ATR 止损后通道离场、N=1%×权益/(2×ATR)、base_buy_amount 上限、trailing_stop=false、T+1/100股整手) / `_note_texts_generic`(未知类型兜底，绝不因缺键崩)；`build_single/compare_dashboard` 中 `initial_cash` 硬下标改 .get；通读 `render_dashboard.py` 确认无写死 KDJ 专用键。未动 turtle.py 策略逻辑。
- QA 严过关独立回归：**路由 NoOne（源码无 Bug，全过）**。35/35 断言 PASS（_note_texts 健壮性 7 + 海龟真实 run 15 + KDJ 真实 run 11 + render_dashboard 扫描 3 + initial_cash 覆盖 2）；额外直接断言 `params_json` 含 type/name 保证 Turtle/KDJ 分支真实命中；交叉运行工程师 `qa_verify_turtle_detail.py` ALL PASS；py_compile 通过；render_dashboard 无遗漏。
- 落地验证脚本：`qa_verify_turtle_detail.py`(工程师)、`qa_edward_independent.py`(QA)。
- 约束延续：本次未重启，**改动下次重启生效**，运行实例当前不影响。

## 架构评审 + 重构：多策略维护结构（团队 software-arch-review-strategy-structure / software-refactor-strategy-structure）
- 用户质疑：新增策略要写"很多文件"，结构是否太繁琐。架构师高见远评审结论：感觉属实但范围被高估——真正要改的中心文件仅 2 个（`strategies/__init__.py` 手写注册表 + `dashboard.py` 按 type 的 if/elif 文案分发）；更隐蔽的坑是"配置自动发现(toml扫描) vs 类手工注册(STRATEGY_REGISTRY)"双发现源不一致（复制 toml 忘注册类→UI能选、执行 KeyError）。完整报告 `docs/strategy-maintenance-review.md`。
- 优化方案对比：A 自动发现 / B 元数据驱动展示 / C 策略自描述接口(=A+B,推荐终态) / 过渡 registry 映射。推荐增量 M1(去展示硬编码)→M2(自动发现)→M3(可选纯配置变体)。
- 用户拍板实施 M1+M2。工程师寇豆码实现（IS_PASS YES，未重启/不起服务）：
  - M1：`turtle.py`/`kdj_macd_dual_entry.py` 各加 `@staticmethod describe(params)`（原 `_note_texts_turtle/_kdj` 文案原样搬入）；`dashboard.py` 的 `_note_texts` 改接口驱动（`get_strategy_class(cfg['type']).describe(params)` 或 generic 兜底），删除 `_note_texts_turtle`/`_note_texts_kdj` 与 `"海龟" in` 名称子串兜底。
  - M2：`__init__.py` 删手写 STRATEGY_REGISTRY dict，改 `discover_strategies()` 扫描 `engine/strategies/*.py` 收集 BaseStrategy 子类以 `cls.type` 建表；`get_strategy_class`/`list_strategies` 签名不变；type 冲突启动抛清晰 TypeError。
- QA 严过关独立回归：**路由 NoOne，35/35 PASS**。turtle/kdj 文案逐字不回退、展示层硬编码全仓 grep 零命中、M2 自动发现生效、加策略零中心改动(临时 demo 验证)、type 冲突清晰报错、py_compile 通过。落地 `qa_verify_autodiscover.py`。
- 重要环境提醒（QA 发现）：存在陈旧副本 `C:\Users\Administrator\WorkBuddy\2026-07-18-09-33-22\strategy_lab`（缺 turtle.py、无 discover_strategies、dashboard 旧版）；**真实代码在 `E:\量化交易\strategy_lab\src`**（editable 安装指向此），评估/改动一律以此为准。
- 约束延续：本次未重启，**重构仅下次重启生效**；当前运行实例(PID 41088)仍跑旧代码。

## 文档同步：M1+M2 重构 + 两项体验修复入档（software-doc-sync-2）
- 用户诉求：「将改动内容更新到文档」。交付总监直接执行文档同步（非成员专业产出，属交付职责）。
- 更新文件（均按本仓库「CHANGELOG 章节 + 文档同步」惯例）：
  - `docs/CHANGELOG.md`：新增「九、2026-07-19 多策略维护结构重构（M1+M2）+ 体验修复」章节，§32 预热中引导、§33 海龟详情 KeyError、§34 M1 去展示硬编码、§35 M2 自动发现，含「文档同步」小节。
  - `README.md` §3.3：移除「在 `__init__.py` 登记注册表」过时步骤，改为自动发现/零中心改动，补可选 `describe()` 说明。
  - `docs/技术文档.md`：§5.1 注册表→自动发现；§9.3 toml `type` 注释改「唯一、自动发现建表」；§13.2 同步移除手工登记、补 `describe()` 与冲突报错；模块表 `__init__.py` 一行改 `discover_strategies()`。
  - `docs/需求文档.md` §1.2：可扩展描述从「加类+登记注册表+写toml」改为自动发现、零中心改动。
  - `docs/strategy-maintenance-review.md`：新增「八、实施状态（2026-07-19 更新）」——M1+M2 已完成、M3 待定、附带修复说明。
- 验证：grep 全仓确认 README/技术文档/需求文档不再有「手工登记注册表」类过时指令（CHANGELOG/review 中的历史描述属正常上下文）。
