# 2026-08-14

## 分析查询页新增「夏普比下限」筛选
- 需求：分析查询页（`/analysis`）加「夏普比下限」查询框（如最低 2）。
- 改动链路（照已有 win_rate_min / profit_factor_min 范式复制）：
  - 前端 `src/strategylab/web.py` `build_analysis_html()`：新增 `#sharpe-min` 输入框（`.f-item` 块，step=0.1）；全局 `curSharpeMin`；`loadRank` 读值 + qs 拼 `sharpe_min`；`loadStats`、`exportCsv` 同样拼参；`resetFilters` 清空 + `change` 监听。
  - 后端 `_api_rank`：解析 `sharpe_min`（`_opt_float`），透传给 `rank_runs`（export 与常规两条调用）。
  - `repository.rank_runs`：新增 `sharpe_min: float|None` 形参 + 去重后过滤 `sharpe is not None and sharpe >= sharpe_min`。
- 验证：`py_compile` 通过；`rank_runs` 实测 dual_ma 共 2211 条，`sharpe_min=2.0` 返回 0 条且 0 条违规（None 正确排除）。
- 结论：筛选参数命名 `sharpe_min`，与既有下限筛选风格一致。
